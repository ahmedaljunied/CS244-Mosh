Mosh experiment setup

Note: git clone must be run on ~

0) Use the CS244 mininet instance as a base
1) update ~/.bashrc by adding this line to the bottom "cd ~/CS244-Mosh"
2) Run cd ~
3) Run git clone https://github.com/ahmedaljunied/CS244-Mosh.git
4) Run cd CS244-Mosh
5) Run mosh-setup.sh which will install mosh and openvpn
6) Copy the private key used to access this amazon instance into stm-data and call it CS244EC2.pem
7) Run run.sh
   A directory will be created that stores the plots. The format of the directory is "results-<timestamp>"
   At the end of the script, the results directory will be displayed
8) Run cd "results-<timestamp>"
9) Plots are .png
